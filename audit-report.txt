================================================================================
INFRASTRUCTURE CMS AUDIT REPORT
Generated against: agent.md rules
Scope: Full repository audit
================================================================================

CRITICAL STAKES NOTICE:
This repository configures a live edge router and core network infrastructure.
All findings below represent real risks to network connectivity and security.
No files have been modified. This is a read-only audit report.

================================================================================
SECTION 1: CALCULABLE DATA VIOLATIONS (Derive Everything Rule)
================================================================================

VIOLATION 1.1 — Hardcoded IPs in data/inventory/network_devices.yaml
----------------------------------------------------------------------
File: data/inventory/network_devices.yaml
Severity: HIGH

The following hardcoded IP addresses violate the "Derive Everything" rule.
IPs must never be stored — they must be derived from `network` + `host` integers.

  devices[0] (nitro):
    ip: "10.64.0.3"         → REMOVE. Derive from: network: 64, host: 3
    wan_mac is fine (conceptual key, not calculable)

  devices[1] (nx):
    ip: "10.64.0.2"         → REMOVE. Derive from: network: 64, host: 2

Proposed Fix:
  Replace `ip:` fields with `network:` and `host:` integer pairs.
  Example:
    - host: "nitro"
      network: 64
      host_id: 3
      mac: "24:2F:D0:D1:2E:30"
      wan_mac: "24:2F:D0:D1:2E:31"

  The compile.py generator or Jinja templates must then derive the IP as:
    10.{{ device.network }}.0.{{ device.host_id }}


VIOLATION 1.2 — Hardcoded IPs in data/networks/vlan-64.yaml
-------------------------------------------------------------
File: data/networks/vlan-64.yaml
Severity: HIGH

The following fields store fully-calculated IP addresses that must be derived:

  dhcp_range:
    start: "10.64.0.100"    → REMOVE. Store only host integer: 100
    end:   "10.64.0.199"    → REMOVE. Store only host integer: 199

  dns:
    - "10.64.0.1"           → REMOVE. Store only host integer: 1
                              (network is already known from `network: 64`)

  static_leases (ALL entries):
    - ip: "10.64.0.2"       → REMOVE. Derive from network: 64, host: 2
    - ip: "10.64.0.3"       → REMOVE. Derive from network: 64, host: 3
    - ip: "10.64.0.4"       → REMOVE. Derive from network: 64, host: 4
    - ip: "10.64.0.10"      → REMOVE. Derive from network: 64, host: 10

Proposed Fix:
  dns:
    - host: 1               # Derived: 10.64.0.1

  dhcp_range:
    start: 100
    end: 199

  static_leases:
    - name: "nx"
      mac: "88:76:B9:8F:30:E0"
      host: 2
    - name: "nitro"
      mac: "24:2F:D0:D1:2E:30"
      host: 3
    - name: "gamecube"
      mac: "84:47:09:44:8B:F9"
      host: 4
    - name: "pi-auditor"
      mac: "74:A7:EA:EE:67:ED"
      host: 10

  The compile.py generator must then derive IPs for leases as:
    10.{{ lease.network | default(vlan.network) }}.0.{{ lease.host }}


VIOLATION 1.3 — Hardcoded IPs in data/networks/vlan-32.yaml
-------------------------------------------------------------
File: data/networks/vlan-32.yaml
Severity: HIGH

  dhcp_range:
    start: "10.32.0.50"     → REMOVE. Store only host integer: 50
    end:   "10.32.0.250"    → REMOVE. Store only host integer: 250

  dns:
    - "1.1.1.1"             → This is an EXTERNAL public DNS server (Cloudflare).
                              This is acceptable as a literal string since it is
                              not a derived internal address. However, it should
                              be moved to a conceptual key like:
                                upstream_dns: ["1.1.1.1", "8.8.8.8"]
                              to make intent explicit.
    - "8.8.8.8"             → Same as above.

Proposed Fix:
  dhcp_range:
    start: 50
    end: 250
  upstream_dns:
    - "1.1.1.1"
    - "8.8.8.8"


VIOLATION 1.4 — Hardcoded IPs in data/services/dns.yaml
---------------------------------------------------------
File: data/services/dns.yaml
Severity: HIGH

The `custom_dns` mapping stores fully-resolved IP addresses. These are
internal addresses that must be derived from network + host integers.

  custom_dns:
    'u64.rsk.al':   10.64.0.1   → network: 64, host: 1
    'nx.rsk.al':    10.64.0.2   → network: 64, host: 2
    'nitro.rsk.al': 10.64.0.3   → network: 64, host: 3
    'dol.rsk.al':   10.64.0.4   → network: 64, host: 4
    'ca.rsk.al':    10.64.0.1   → network: 64, host: 1
    '*.rsk.al':     10.64.0.1   → network: 64, host: 1

Proposed Fix:
  Replace the flat IP map with a structured list:

  custom_dns:
    - hostname: "u64.rsk.al"
      network: 64
      host: 1
    - hostname: "nx.rsk.al"
      network: 64
      host: 2
    - hostname: "nitro.rsk.al"
      network: 64
      host: 3
    - hostname: "dol.rsk.al"
      network: 64
      host: 4
    - hostname: "ca.rsk.al"
      network: 64
      host: 1
    - hostname: "*.rsk.al"
      network: 64
      host: 1

  compile.py must then derive IPs and build the flat map for Jinja consumption.
  The blocky.yml.j2 template already iterates custom_dns as a dict — it would
  need updating to match the new structure.


VIOLATION 1.5 — Hardcoded IP in data/services/headscale.yaml
-------------------------------------------------------------
File: data/services/headscale.yaml
Severity: MEDIUM

  mesh_gateway_ip: 10.64.0.4   → REMOVE. Derive from: network: 64, host: 4

  mesh_subnet: 100.64.0.0/24   → This is the Headscale mesh subnet (not a
                                  derived internal address). It is a conceptual
                                  value for the VPN overlay network and is
                                  acceptable as a literal. However, it could be
                                  split into:
                                    mesh_network: "100.64.0"
                                    mesh_prefix: 24
                                  for consistency. This is a minor concern.

Proposed Fix:
  mesh_gateway_network: 64
  mesh_gateway_host: 4
  # compile.py derives: mesh_gateway_ip = 10.64.0.4


VIOLATION 1.6 — Hardcoded CIDR in data/services/firewall.yaml (indirect)
-------------------------------------------------------------------------
File: ansible/roles/firewall/templates/nftables.conf.j2
Severity: MEDIUM

  define SANDBOX_NET = 10.32.0.0/24

  This is hardcoded directly in the Jinja template. The sandbox network CIDR
  must be derived from the networks data model (vlan-32), not hardcoded in
  the template.

  define BREAKGLASS_VPN_NET = 100.64.1.0/24

  This is also hardcoded. It should reference infra_services.vpn.cidr which
  already exists in data/services/vpn.yaml as `cidr: 100.64.1.0/24`.
  However, even that stored CIDR in vpn.yaml is a violation — it should be
  derived from a prefix + mask, not stored as a full CIDR string.

Proposed Fix (template):
  define SANDBOX_NET = {{ networks['vlan-32'].cidr }}
  define BREAKGLASS_VPN_NET = {{ infra_services.vpn.cidr }}

  And in data/services/vpn.yaml, replace:
    cidr: 100.64.1.0/24
  with:
    prefix: "100.64.1"
    prefix_len: 24
  Then derive cidr in compile.py as: f"{vpn.prefix}.0/{vpn.prefix_len}"


================================================================================
SECTION 2: IMPLEMENTATION LEAKAGE VIOLATIONS
================================================================================

VIOLATION 2.1 — Tool-specific key in data/nodes/ultra64.yaml
-------------------------------------------------------------
File: data/nodes/ultra64.yaml
Severity: LOW-MEDIUM

  host_services.dhcp.security_profile: hardened
  host_services.vpn.security_profile: strict

  The `security_profile` key inside `host_services` sub-objects is an
  implementation/policy concept that belongs to the service definition
  (data/services/dhcp.yaml, data/services/vpn.yaml), not the node's
  host_services binding. The node should only declare WHAT services it runs
  and their instance-specific parameters (ports, keys, ranges), not re-declare
  the security profile that is already owned by the service definition.

Proposed Fix:
  Remove `security_profile` from host_services entries in node files.
  Security profiles are owned by data/services/*.yaml exclusively.


VIOLATION 2.2 — `netmask` key in network files
-----------------------------------------------
File: data/networks/vlan-64.yaml, data/networks/vlan-32.yaml
Severity: LOW

  netmask: "255.255.255.0"

  This is a calculable/derived value. A /24 subnet mask is always
  255.255.255.0. It should not be stored — it should be derived from the
  prefix length. The prefix length itself (24) is also implicit given the
  current architecture always uses /24. If variable prefix lengths are ever
  needed, store `prefix_len: 24` and derive the netmask in compile.py or Jinja.

Proposed Fix:
  Remove `netmask` from network YAML files.
  Derive in compile.py: subnet_mask = "255.255.255.0" (for /24 networks).
  Or store `prefix_len: 24` and derive programmatically.


VIOLATION 2.3 — `interface` key in network files may leak implementation
------------------------------------------------------------------------
File: data/networks/vlan-64.yaml → interface: "eth1"
File: data/networks/vlan-32.yaml → interface: "eth1.32"
Severity: MEDIUM

  The `interface` key stores a Linux network interface name (eth1, eth1.32).
  This is OS/implementation-specific. The node's `interfaces` block in
  data/nodes/ultra64.yaml already owns the canonical interface names
  (lan.dev: enp1s0f1). The network file should not duplicate or assume
  interface names — this creates a split ownership of interface assignment.

  Furthermore, `eth1` conflicts with the node's actual LAN interface name
  `enp1s0f1`. This is a latent bug.

Proposed Fix:
  Remove `interface` from network YAML files.
  Interface-to-network binding belongs exclusively in the node file under
  `interfaces:`. Templates should reference `interfaces.lan.dev` from the
  node, not from the network definition.


================================================================================
SECTION 3: DUPLICATION / SINGLE SOURCE OF TRUTH VIOLATIONS
================================================================================

VIOLATION 3.1 — Static lease IPs defined in both vlan-64.yaml and nodes
------------------------------------------------------------------------
Files: data/networks/vlan-64.yaml AND data/inventory/network_devices.yaml
       AND data/nodes/ultra64.yaml (host_services.dhcp.static_leases)
Severity: HIGH

  The IP/MAC/hostname mappings for nitro, nx, gamecube, pi-auditor appear in:
    1. data/networks/vlan-64.yaml → static_leases (with IPs)
    2. data/inventory/network_devices.yaml → devices (with IPs and MACs)
    3. data/nodes/ultra64.yaml → host_services.dhcp.static_leases
       (nitro: network:64/host:3/mac, nx: network:64/host:2/mac)

  This is a three-way duplication of the same facts. The MAC address for
  nitro (24:2F:D0:D1:2E:30) appears in BOTH network_devices.yaml AND
  ultra64.yaml's static_leases. The MAC for nx (88:76:B9:8F:30:E0) also
  appears in both places.

  Single Source of Truth Rule: Each device's MAC and host assignment must
  have exactly ONE owner.

Proposed Fix:
  - data/networks/vlan-64.yaml should be the SOLE owner of static DHCP
    lease definitions (MAC → host integer mappings) for that network.
  - data/inventory/network_devices.yaml should reference node identifiers,
    not duplicate MAC/IP data.
  - data/nodes/ultra64.yaml host_services.dhcp.static_leases should be
    REMOVED entirely. The DHCP role should read lease data from the network
    definition (vlan-64.yaml), not from the node running the DHCP server.
  - compile.py should inject the network's static_leases into the DHCP
    host_services config when building the inventory.


VIOLATION 3.2 — VPN prefix/CIDR split ownership
------------------------------------------------
Files: data/services/vpn.yaml AND ansible/roles/vpn/templates/wg0.conf.j2
Severity: MEDIUM

  data/services/vpn.yaml stores:
    prefix: 100.64.1
    cidr: 100.64.1.0/24

  The `cidr` is directly derivable from `prefix` + `/24`. Storing both
  violates the Derive Everything rule. The cidr should be computed by
  compile.py, not stored.

Proposed Fix:
  Remove `cidr` from data/services/vpn.yaml.
  In compile.py, derive: vpn_cidr = f"{vpn_data['prefix']}.0/24"
  Inject as `infra_services.vpn.cidr` into the inventory for template use.


VIOLATION 3.3 — Domain names duplicated across caddy.yaml and dns.yaml
-----------------------------------------------------------------------
Files: data/services/caddy.yaml AND data/services/dns.yaml
Severity: MEDIUM

  data/services/dns.yaml custom_dns contains explicit hostname→IP mappings
  for hosts that are ALSO defined as Caddy service domains:
    'u64.rsk.al', 'nx.rsk.al', 'nitro.rsk.al', etc.

  The canonical hostname for each node is owned by the node's identity block.
  The DNS service should derive its custom_dns entries from node identity data,
  not maintain a parallel list. This creates a situation where adding a new
  node requires updating both the node file AND dns.yaml.

  Note: The wildcard '*.rsk.al' → 10.64.0.1 entry in dns.yaml effectively
  covers all rsk.al subdomains pointing to the router. The individual hostname
  entries (u64, nx, nitro, dol) are therefore partially redundant with the
  wildcard, EXCEPT for hosts that need to resolve to a different IP than the
  router. This should be made explicit.

Proposed Fix:
  - The wildcard '*.rsk.al' → router (network:64, host:1) should remain as
    the catch-all in dns.yaml.
  - Individual host entries that resolve to the ROUTER (u64, ca) are redundant
    with the wildcard and should be removed.
  - Entries for hosts at different IPs (nx→host:2, nitro→host:3, dol→host:4)
    should be derived from node data by compile.py, not hardcoded in dns.yaml.


================================================================================
SECTION 4: NAMESPACE RULE VIOLATIONS
================================================================================

VIOLATION 4.1 — Mixed namespace in data/services/caddy.yaml (headscale service)
---------------------------------------------------------------------------------
File: data/services/caddy.yaml
Severity: LOW

  - name: headscale_wan
    domains: ["wfc.raskal.io"]   ✓ Correct — public WAN domain

  All other WAN services use raskal.io ✓
  All LAN services use rsk.al ✓

  No namespace violations found in caddy.yaml domain assignments.
  The namespace separation is correctly implemented throughout caddy.yaml.


VIOLATION 4.2 — auth service appears on BOTH namespaces (by design, but verify)
---------------------------------------------------------------------------------
File: data/services/caddy.yaml
Severity: INFO (not a violation, flagged for review)

  auth_wan:  domains: ["auth.raskal.io"]  ✓ public
  auth_lan:  domains: ["auth.rsk.al"]     ✓ internal

  This dual-namespace pattern for auth is intentional (Authelia must be
  reachable from both LAN and WAN for forward-auth redirects). This is
  architecturally correct per the namespace rules.

  No violation — flagged for awareness only.


================================================================================
SECTION 5: TEMPLATE COMPLIANCE VIOLATIONS
================================================================================

VIOLATION 5.1 — Hardcoded SANDBOX_NET in nftables.conf.j2
----------------------------------------------------------
File: ansible/roles/firewall/templates/nftables.conf.j2
Severity: HIGH

  define SANDBOX_NET = 10.32.0.0/24

  This is hardcoded. It must reference the derived network data:
    define SANDBOX_NET = {{ networks['vlan-32'].cidr }}

  The `networks` variable IS already injected by compile.py into
  inventory all.vars, so this fix is straightforward.


VIOLATION 5.2 — Hardcoded BREAKGLASS_VPN_NET in nftables.conf.j2
-----------------------------------------------------------------
File: ansible/roles/firewall/templates/nftables.conf.j2
Severity: HIGH

  define BREAKGLASS_VPN_NET = 100.64.1.0/24

  Must reference: {{ infra_services.vpn.cidr }}
  (after compile.py derives and injects it)


VIOLATION 5.3 — kea-dhcp4.conf.j2 references static_leases with `hostname` key
--------------------------------------------------------------------------------
File: ansible/roles/dhcp/templates/kea-dhcp4.conf.j2
Severity: MEDIUM

  {% for lease in host_services.dhcp.static_leases %}
      "hostname": "{{ lease.hostname }}"

  The static_leases in data/nodes/ultra64.yaml use key `hostname`:
    - hostname: nitro
  But data/networks/vlan-64.yaml uses key `name`:
    - name: "nitro"

  These are inconsistent. The template references `lease.hostname` but the
  network-level data uses `lease.name`. If static_leases ownership is moved
  to vlan-64.yaml (per fix for Violation 3.1), the template must use
  `lease.name` OR the network data must be updated to use `hostname`.

  Additionally, the template references `lease.ip` which will be a derived
  value once Violation 1.2 is fixed. compile.py already handles this
  derivation in the `static_leases` processing block — this is correct.

Proposed Fix:
  Standardize on `hostname` as the key in both node and network lease
  definitions. Update vlan-64.yaml to use `hostname:` instead of `name:`.


VIOLATION 5.4 — unbound.conf.j2 uses undefined variables with unsafe defaults
------------------------------------------------------------------------------
File: ansible/roles/dns/templates/unbound.conf.j2
Severity: MEDIUM

  chroot: {{ unbound_chroot | default('""') }}
  do-not-query-localhost: {{ unbound_do_not_query_localhost | default('no') }}

  These variables (`unbound_chroot`, `unbound_do_not_query_localhost`) are
  not defined anywhere in the data model (not in environment.yaml, not in
  dns.yaml, not in any node file). They rely entirely on Jinja defaults.

  This means the unbound configuration is partially outside the CMS data
  model. These values should be defined in data/services/dns.yaml under
  an `unbound:` config block.

Proposed Fix:
  Add to data/services/dns.yaml:
    unbound:
      chroot: ""
      do_not_query_localhost: no

  Update template to:
    chroot: {{ infra_services.dns.unbound.chroot }}
    do-not-query-localhost: {{ infra_services.dns.unbound.do_not_query_localhost }}


VIOLATION 5.5 — wg0.conf.j2 constructs IP inline rather than using derived value
---------------------------------------------------------------------------------
File: ansible/roles/vpn/templates/wg0.conf.j2
Severity: LOW

  Address = {{ infra_services.vpn.prefix }}.{{ host_services.vpn.host_id }}/24

  The /24 prefix length is hardcoded in the template. It should reference
  a prefix_len value from the service data:
    Address = {{ infra_services.vpn.prefix }}.{{ host_services.vpn.host_id }}/{{ infra_services.vpn.prefix_len }}

  Also:
  AllowedIPs = {{ infra_services.vpn.prefix }}.{{ peer.host_id }}/32, {{ networks['vlan-64'].cidr }}

  The /32 host mask is hardcoded. This is acceptable for a host route (/32
  is always correct for a single peer IP), but the vlan-64 CIDR reference
  is correct and good practice.


VIOLATION 5.6 — 30-lan.network.j2 hardcodes /24 prefix
--------------------------------------------------------
File: ansible/roles/network/templates/30-lan.network.j2
Severity: LOW

  Address={{ ip }}/24

  The /24 is hardcoded. Should reference the network's prefix length:
    Address={{ ip }}/{{ networks[node_network_id].prefix_len | default(24) }}

  This is low severity since the architecture is consistently /24, but it
  violates the derive-everything principle.


================================================================================
SECTION 6: LOGICAL & ARCHITECTURAL CONSISTENCY
================================================================================

FINDING 6.1 — DNS Resolution Chain Analysis
--------------------------------------------
Chain: Client → Blocky (port 53) → Unbound (port 5335) → Upstream resolvers

  blocky.yml.j2:
    upstream.default: 127.0.0.1:5335   ✓ Correct — routes to local Unbound

  unbound.conf.j2:
    interface: 127.0.0.1, port: 5335   ✓ Correct — listens on loopback
    access-control: 127.0.0.0/8 allow  ✓ Correct — only local access

  Blocky HTTP port: 4000 (for DoH)     ✓ Matches caddy backend_port in dns_wan
                                          and dns_lan services

  ISSUE: Blocky listens on port 53 (UDP/TCP) per blocky.yml.j2 `port: 53`.
  The firewall (nftables.conf.j2) allows:
    iifname "{{ interfaces.lan.dev }}" udp dport { 53, 67 } accept
    iifname "{{ interfaces.lan.dev }}" tcp dport 53 accept
  ✓ DNS from LAN is allowed.

  ISSUE: There is NO firewall rule allowing DNS queries from the WAN interface
  to reach Blocky. The DoH endpoint (port 4000/443 via Caddy) is the only
  external DNS path, which is correct by design. Direct port 53 from WAN
  is correctly blocked. ✓ Architecture is sound.

  ISSUE: The `custom_dns` in blocky.yml.j2 iterates as a dict:
    {% for host, ip in infra_services.dns.custom_dns.items() %}
  If custom_dns is restructured to a list (per Violation 1.4 fix), this
  template MUST be updated simultaneously. Failure to do so will break DNS.
  This is a CRITICAL coordination requirement for the fix.


FINDING 6.2 — Headscale Routing Logic
--------------------------------------
File: data/services/headscale.yaml, ansible/roles/firewall/templates/nftables.conf.j2

  mesh_subnet: 100.64.0.0/24    (Headscale coordination network)
  mesh_gateway_ip: 10.64.0.4    (gamecube/Proxmox host — the exit node)

  Firewall rules:
    define HEADSCALE_NET = {{ infra_services.headscale.mesh_subnet }}  ✓
    ip saddr $HEADSCALE_NET ip daddr $VAULT_NET accept  ✓
    ip saddr $VAULT_NET ip daddr $HEADSCALE_NET accept  ✓

  WireGuard VPN (Breakglass) subnet: 100.64.1.0/24
  Headscale mesh subnet:             100.64.0.0/24

  These are DIFFERENT /24 subnets within 100.64.0.0/10 (CGNAT space).
  They do not overlap. ✓ No routing conflict.

  ISSUE: The Headscale DERP port is referenced in nftables.conf.j2:
    udp dport {{ infra_services.headscale.derp.port }}
  But data/services/headscale.yaml does NOT define a `derp:` block or
  `derp.port`. This will cause an Ansible/Jinja rendering FAILURE.

  This is a CRITICAL BUG — the firewall template will fail to render,
  leaving the router with NO firewall rules (flush ruleset runs first).

Proposed Fix:
  Add to data/services/headscale.yaml:
    derp:
      port: 3478
  (3478 is the standard STUN port used by Headscale DERP)
  Verify the actual port in use and set accordingly.


FINDING 6.3 — DHCP Template vs Node Data Structural Mismatch
-------------------------------------------------------------
Files: data/nodes/ultra64.yaml, ansible/roles/dhcp/templates/kea-dhcp4.conf.j2

  The node defines DHCP range as host integers:
    range_start: 100
    range_end: 200

  compile.py correctly expands these:
    dhcp_config['range_start'] = f"10.{dhcp_network}.0.{dhcp_config['range_start']}"
    dhcp_config['range_end']   = f"10.{dhcp_network}.0.{dhcp_config['range_end']}"

  The template then uses:
    "pool": "{{ host_services.dhcp.range_start }} - {{ host_services.dhcp.range_end }}"
  ✓ This is correct after compile.py expansion.

  HOWEVER: The static_leases in the node file use `hostname` key:
    - hostname: nitro
  But the Kea config requires `hostname` in reservations. ✓ Key name matches
  template. But as noted in Violation 3.1, these leases are duplicated from
  vlan-64.yaml which uses `name` instead of `hostname`. This inconsistency
  must be resolved.


FINDING 6.4 — Ansible Role Execution Order (site.yml)
------------------------------------------------------
File: ansible/site.yml

  Execution order for router profile:
    1. base
    2. chrony
    3. network      ← configures interfaces
    4. firewall     ← applies nftables rules
    5. dns          ← starts Blocky + Unbound
    6. dhcp         ← starts Kea
    7. vpn          ← configures WireGuard
    8. avahi        ← mDNS
    9. step-ca      ← internal CA
    10. caddy       ← reverse proxy

  ISSUE: `network` role runs before `firewall`. If the network role triggers
  a network restart (via handlers), interfaces may briefly be up without
  firewall rules. This is a minor race condition risk on initial provisioning,
  not a logical flaw in steady-state operation.

  ISSUE: `dns` runs before `caddy`. This is CORRECT — Caddy's DoH backends
  depend on Blocky being up. ✓

  ISSUE: `step-ca` runs before `caddy`. This is CORRECT — Caddy needs the
  ACME endpoint from step-ca for sovereign TLS. ✓

  ISSUE: `vpn` runs before `avahi`. Order is fine — no dependency between them.

  ISSUE: The `base` role runs on ALL hosts (no `when` guard), while router
  roles are gated on `profile == 'router'`. This is correct architecture. ✓

  ISSUE: `chrony` also runs on ALL hosts with no profile guard. This is
  intentional (all nodes need time sync) and correct. ✓

  OVERALL: Execution order is logically sound with the minor race condition
  caveat noted above.


FINDING 6.5 — WAN Interface MAC Address Ownership
--------------------------------------------------
Files: data/nodes/ultra64.yaml, data/inventory/network_devices.yaml

  ultra64.yaml:
    interfaces.wan.mac: "24:2F:D0:D1:2E:31"   (WAN MAC)

  network_devices.yaml (nitro entry):
    wan_mac: "24:2F:D0:D1:2E:31"              (SAME MAC)

  CRITICAL DUPLICATION: The WAN MAC address 24:2F:D0:D1:2E:31 appears in
  BOTH ultra64.yaml (as the router's WAN interface MAC) AND in
  network_devices.yaml (as nitro's wan_mac).

  This is almost certainly a DATA ERROR. The TP-Link Archer AXE75 (nitro)
  is an Access Point/Bridge, not the edge router. The WAN MAC of the router
  (ultra64) should NOT match the wan_mac of the AP (nitro).

  This suggests either:
    a) The wan_mac in network_devices.yaml for nitro is incorrect, OR
    b) The interfaces.wan.mac in ultra64.yaml is incorrect.

  This is a CRITICAL DATA INTEGRITY BUG. The 10-wan.link.j2 template uses
  this MAC to pin the WAN interface:
    MACAddress={{ interfaces.wan.mac }}
  If this MAC is wrong, the WAN interface will not come up correctly.

Proposed Fix:
  Verify the actual hardware MACs and correct whichever entry is wrong.
  The router's WAN NIC MAC and the AP's WAN MAC must be different physical
  devices and therefore different MAC addresses.


FINDING 6.6 — Caddy auth_profiles backend derivation in compile.py
-------------------------------------------------------------------
File: compile.py, data/services/caddy.yaml

  compile.py processes caddy services backends:
    if service_name == 'caddy' and 'services' in processed_service:
      for caddy_service in processed_service['services']:
        if 'backend_network' in caddy_service and 'backend_host' in caddy_service:
          backend_ip = derive_ip_from_network_host(...)
          caddy_service['backend'] = f"{backend_ip}:{caddy_service['backend_port']}"

  BUT: compile.py does NOT process `auth_profiles` backends in caddy.yaml.
  The auth_profiles also have backend_network and backend_host:
    auth_gate:
      backend_network: 64
      backend_host: 203
      backend_port: 9091

  The Caddyfile.j2 template renders auth profiles as:
    forward_auth 10.{{ profile.backend_network }}.0.{{ profile.backend_host }}:{{ profile.backend_port }}

  This works because the template does the IP construction inline. However,
  this is inconsistent with how service backends are handled (compile.py
  pre-derives them). The auth_profiles should also have their backends
  pre-derived by compile.py for consistency, OR the services should also
  use inline construction like auth_profiles do.

  This is an architectural inconsistency, not a functional bug.


FINDING 6.7 — data/networks/dhcp.yaml purpose is unclear / potentially redundant
---------------------------------------------------------------------------------
File: data/networks/dhcp.yaml

  This file contains:
    vlans:
      - vlan-64
      - vlan-32

  This appears to be a DHCP policy index listing which VLANs have DHCP
  enabled. However, compile.py does NOT read or process this file at all.
  The DHCP configuration is driven entirely by host_services.dhcp in the
  node file (ultra64.yaml).

  This file is currently dead data — it is loaded by load_networks_data()
  as a network file (since it's in data/networks/), but its `vlans` key
  is not used anywhere in compile.py or any template.

  If this is intended as a DHCP policy index, it needs to be consumed by
  compile.py. If it is documentation only, it should be moved or clearly
  marked as such.

Proposed Fix:
  Either:
    a) Implement compile.py logic to use this as the authoritative list of
       DHCP-enabled networks, driving DHCP config generation from vlan-*.yaml
       data rather than from node host_services.
    b) Move to a docs/ directory and mark as reference only.
    c) Delete if superseded by the node-driven approach.


================================================================================
SECTION 7: SUMMARY OF FINDINGS BY SEVERITY
================================================================================

CRITICAL (must fix before deployment):
  - FINDING 6.2: headscale.derp.port missing → firewall template render FAILURE
  - FINDING 6.5: Duplicate WAN MAC (24:2F:D0:D1:2E:31) in ultra64 + nitro → DATA ERROR
  - VIOLATION 3.1: Static lease triple-duplication across 3 files

HIGH:
  - VIOLATION 1.1: Hardcoded IPs in network_devices.yaml
  - VIOLATION 1.2: Hardcoded IPs in vlan-64.yaml
  - VIOLATION 1.3: Hardcoded IPs in vlan-32.yaml
  - VIOLATION 1.4: Hardcoded IPs in dns.yaml custom_dns
  - VIOLATION 5.1: Hardcoded SANDBOX_NET in nftables template
  - VIOLATION 5.2: Hardcoded BREAKGLASS_VPN_NET in nftables template
  - FINDING 6.1: custom_dns template/data structure must be updated atomically

MEDIUM:
  - VIOLATION 1.5: Hardcoded mesh_gateway_ip in headscale.yaml
  - VIOLATION 2.1: security_profile leaking into node host_services
  - VIOLATION 2.3: interface key in network files (conflicts with node data)
  - VIOLATION 3.2: VPN cidr stored AND derivable from prefix
  - VIOLATION 3.3: Domain/hostname duplication across caddy.yaml and dns.yaml
  - VIOLATION 5.3: static_leases key inconsistency (hostname vs name)
  - VIOLATION 5.4: unbound config vars not in data model
  - FINDING 6.3: DHCP lease key mismatch between node and network files
  - FINDING 6.6: auth_profiles backends not pre-derived by compile.py
  - FINDING 6.7: data/networks/dhcp.yaml is dead/unused data

LOW:
  - VIOLATION 2.2: netmask stored (derivable from prefix_len)
  - VIOLATION 5.5: /24 hardcoded in wg0.conf.j2
  - VIOLATION 5.6: /24 hardcoded in 30-lan.network.j2
  - FINDING 6.4: Minor network/firewall race condition on initial provisioning

================================================================================
END OF AUDIT REPORT
================================================================================
